// @flow

export type Store = {
  name: string,
  newStore: boolean,
  offer: string,
  deals: number,
  logo: string,
  url: string,
  couponActive: boolean
};

export type Categories = {
  title: string,
  number: number,
};
